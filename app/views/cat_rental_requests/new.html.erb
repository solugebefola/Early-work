<h1>RENT A CAT!!!!</h1>

<form action="<%= cat_rental_requests_url %> " method="POST">
  <input
    type="hidden"
    name="authenticity_token"
    value="<%= form_authenticity_token %>">

  <select name="cat_rental_request[cat_id]" ><br>
    <option value=""></option>
    <% @cats.each do |cat| %>
      <option value="<%= cat.id %>"> <%= cat.name %> </option>
    <% end %>
  </select>
  <br><br><input type="date" name="cat_rental_request[start_date]" value="<%= @cat_rental_request.start_date %>"><br>
  <input type="date" name="cat_rental_request[end_date]" value="<%= @cat_rental_request.end_date %>"><br><br>
  <input type="submit" value="Submit rental request">
</form>

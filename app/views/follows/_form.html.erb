<% follow_text = current_user.follows?(user) ? "followed" : "unfollowed"  %>

<button
  data-user-id="<%= user.id %>"
  data-initial-follow-state="<%= follow_text %>"
  class="follow-toggle">
</button>


<script src="">

</script>

var Autocomplete = React.createClass({
  render: function() {
    var matchedNames = []

    names.forEach ( function (el) {
      if (el ~ ){
        
      }
    });


    return (
      <div>
        <input type={"text"} className={"autovalue"}>
        <ul>
          {matchedNames}
        </ul>
      </div>
    )
  };
});
